export function Footer() {
  return (
    <footer className="footer">
      <span>Phase 1 MVP</span>
      <span>STL files are analyzed locally in your browser before submission features are added.</span>
    </footer>
  );
}
